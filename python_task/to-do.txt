+ добавить вывод в xml чтобы был выбор в какой формат сохранять.
+ после того, как загрузила не выключается и предлагет перезагрузить в другой файл.

+ сделать докер с докер компоузом

будет одна репа на все задачи но каждая таска будет в отдельной папке
будет мейн ветка и ветка под каждую задачу. Потому пулл request
Потом после апрува будем мерджить в одну мейн ветку

+ .env - вынести конекшены

1. добавить unit-тестирование (если успею)(на каждый класс)
2. переделать логику


+ Места по типу "cursor.execute("""
                CREATE TABLE IF NOT EXISTS rooms (
                    id INT PRIMARY KEY,
                    name VARCHAR(255)
                );
            """)" занести в переменные, если они таковыми не являются, чтобы было например cursor.execute('rooms_table')
